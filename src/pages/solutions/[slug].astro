---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import SimplePageContent from '../../components/SimplePageContent.astro';
import { solutionPages } from '../../data/site';

export function getStaticPaths() {
  return solutionPages.map((page) => ({
    params: { slug: page.slug },
    props: { page },
  }));
}

const { page } = Astro.props;
---

<BaseLayout title={page.title} description={page.description} canonical={`/solutions/${page.slug}/`}>
  <PageHero title={page.heroTitle} description={page.heroText} image={page.heroImage} eyebrow="Family needs" />
  <SimplePageContent page={page} />
</BaseLayout>
