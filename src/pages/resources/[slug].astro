---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import SimplePageContent from '../../components/SimplePageContent.astro';
import { resourcePages } from '../../data/site';

export function getStaticPaths() {
  return resourcePages.map((page) => ({
    params: { slug: page.slug },
    props: { page },
  }));
}

const { page } = Astro.props;
---

<BaseLayout title={page.title} description={page.description} canonical={`/resources/${page.slug}/`}>
  <PageHero title={page.heroTitle} description={page.heroText} image={page.heroImage} eyebrow="Parent resource" />
  <SimplePageContent page={page} />
</BaseLayout>
